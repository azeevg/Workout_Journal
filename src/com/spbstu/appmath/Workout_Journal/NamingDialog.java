package com.spbstu.appmath.Workout_Journal;

import android.app.AlertDialog;
import android.content.Context;

public class NamingDialog extends AlertDialog {
    protected NamingDialog(Context context) {
        super(context);
    }

    protected NamingDialog(Context context, int theme) {
        super(context, theme);
    }

    protected NamingDialog(Context context, boolean cancelable, OnCancelListener cancelListener) {
        super(context, cancelable, cancelListener);
    }
}
